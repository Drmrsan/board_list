<div class="container">
	<h1>Board List</h1>
	<%= link_to "New Board", new_board_path, class:"btn btn-sm btn-success" %>
	<div class="row">
		<% @boards.each do |board| %>
			<div class="col-sm-2 well box">
				<%= link_to board.title , board %>
			</div>
		<% end %>
	</div>
</div>

