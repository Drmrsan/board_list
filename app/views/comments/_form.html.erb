<div class="comment_form">
	<%= simple_form_for([@board,@to_do,@card,@card.comments.build]) do |f| %>
		<%= f.input :comment, placeholder: "Leave comment...", label: false %>
		<%= f.button :submit, class: "btn btn-sm btn-warning" %>
	<% end %>
</div>
